import { create, NButton, NLayout, NLayoutSider, NLayoutContent, NDataTable, NMessageProvider } from 'naive-ui'

export const naive = create({
  components: [NButton, NLayout, NLayoutSider, NLayoutContent, NDataTable, NMessageProvider],
})
